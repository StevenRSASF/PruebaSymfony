{{ form_start(form) }}
    <div class="form-style">
    <div class="form-group pb-3">
        Titulo:</br>
        {{ form_widget(form.titulo) }}
    </div>
    <div class="form-group pb-3">
        Descripcion:</br>
        {{ form_widget(form.descripcion) }}
    </div>      
    <div class="form-group pb-3">
        Año de publicacion:</br>
        {{ form_widget(form.anioPublicacion) }}
    </div>
    <div class="form-group pb-3">
        Autor:</br>
        {{ form_widget(form.autor) }}
    </div>
    <div class="form-group pb-3">
    <button class="btn btn-secondary w-100 font-weight-bold mt-2">{{ button_label|default('Guardar') }}</button>
    </div>
    </div>
   
{{ form_end(form) }}
